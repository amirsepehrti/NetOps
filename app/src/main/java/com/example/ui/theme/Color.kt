package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Low-Fatigue, High Density Nord Slate Theme Palette
val CyberBlack = Color(0xFF1B1E24) // Soothing deep-slate background (reduced contrast)
val CyberDark = Color(0xFF15171D)  // Nav/AppBar background
val CyberGray = Color(0xFF252932)  // Soft contrast card background
val CyberSlate = Color(0xFF2E3440) // Secondary elements and borders

val ConsoleGreen = Color(0xFFA3BE8C)      // Soft sage green for gateway/uptime (very low fatigue)
val ConsoleGreenLight = Color(0xFFECEFF4) // Soft snow/white for highlights
val ConsoleGreenDim = Color(0xFF3D4C38)   // Muted, dim green border

val CyberCyan = Color(0xFF88C0D0)    // Gentle frost blue/teal accent
val CyberCyanDim = Color(0xFF283A41) // Dim frost container

val CyberCrimson = Color(0xFFD08770)    // Soft peach/terracotta for warnings/errors (no harsh reds)
val CyberCrimsonDim = Color(0xFF4C3E3B) // Dimmed warm amber container

val OffWhite = Color(0xFFECEFF4)    // Softer off-white for main headers
val Silver = Color(0xFFD8DEE9)      // Gentle gray-blue body text
val DarkSilver = Color(0xFF969FAA)  // Relaxed slate gray for labels and secondary details

